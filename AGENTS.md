# Repository working instructions

## Command-line operations and GitHub

- The user performs all command-line operations. Do not run shell commands, terminal commands, scripts, package installation commands, tests, or Git commands on their behalf.
- Exception: read-only commands may inspect this repository and user-provided file attachments, including file contents, directory listings, and Git status or diffs. Do not modify files through commands, install packages, run tests or analysis tools, execute project scripts, or perform Git mutations. Those command-line operations remain the user's responsibility.
- When command-line work is needed, provide the exact commands for the user to run, explain their purpose briefly, and wait for the results when further work depends on them.
- The user always pushes to GitHub personally. Never push to GitHub on the user's behalf.
- Direct file inspection and editing through non-command-line tools are permitted within the requested task scope.

## Project boundaries

- This repository is the authoritative BABBD postseason fantasy baseball project, distinct from the user's basketball project.
- Do not access or modify `C:\Users\Michael\Documents\Apps\Baseball` unless the user explicitly requests it as a historical reference.
